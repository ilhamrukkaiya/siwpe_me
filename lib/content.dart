import 'package:flutter/material.dart';

const hdText =
    TextStyle(color: Colors.blue, fontSize: 25, fontWeight: FontWeight.bold);
const size = SizedBox(
  height: 20,
);
const frgettext = TextStyle(color: Colors.blue, fontSize: 18);
